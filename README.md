# Hyprland_dotfiles

项目简介
- 本仓库包含 Hyprland 窗口管理器的配置文件（dotfiles），用于定制桌面环境、快捷键、主题与启动脚本。

安装与使用
- 将配置同步到 `~/.config/hypr/`（或按仓库说明），重启 Hyprland 生效。

注意
- 配置可能依赖特定的工具（例如 `waybar`, `swaybg` 等），请在使用前检查依赖并安装。
